[package]
name = "greeter_jni"
version = "1.0.0"
edition = "2024"
license = "MIT OR Apache-2.0"

[lib]
crate-type = ["cdylib"]

[dependencies]
jni = "0.22.4"

[profile.release]
strip = "symbols"
lto = true
codegen-units = 1
